<!--index.wxml-->
<view class="root">
  <view bindtap="getImageList">点击</view>
  <view>
	<view wx:for="{{imageList}}" wx:for-item="item" wx:key="index">
		<view>{{item.author}}</view>
		<!-- <image src="{{item.url}}"></image> -->
	</view>
  </view>
</view>
